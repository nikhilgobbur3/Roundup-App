COMPOSIO_API_KEY=
# Optional: pin which Hashnode publication to publish to. If empty, the first
# publication found on the connected Hashnode account is used automatically.
HASHNODE_PUBLICATION_ID=

# Optional: Notion database used by scripts/tracking/notion-sync.mjs. If empty,
# the sync searches Notion for a database titled "RoundUp Tracking".
NOTION_DATABASE_ID=

# Cold email outreach (scripts/email/) — emails are queued as GitHub issues for
# manual sending (no Brevo). The daily bot opens "Email: <Company>" issues with
# the recipient + subject + body; send it from your own email and comment `done`.
EMAIL_SEND_PER_DAY=
EMAIL_COMPANIES_PER_DAY=
